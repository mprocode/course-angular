import { CloneDirective } from './clone.directive';

describe('CloneDirective', () => {
  it('should create an instance', () => {
    const directive = new CloneDirective();
    expect(directive).toBeTruthy();
  });
});
