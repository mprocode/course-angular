import { BluefyDirective } from './bluefy.directive';

describe('BluefyDirective', () => {
  it('should create an instance', () => {
    const directive = new BluefyDirective();
    expect(directive).toBeTruthy();
  });
});
